.Wrapper {
  width: 100%;
  float: left;
}
.Inner {
  width: calc(100% - 32px);
  padding: 0px 16px;
}
.ButtonContainer {
  width: 100%;
  height: 62px;
  max-width: var(--main-max-width);
  padding: 16px 0px;
  box-sizing: border-box;
}
maki-icon-cross{
  --icon-color:var(--icon-red-color);
}
