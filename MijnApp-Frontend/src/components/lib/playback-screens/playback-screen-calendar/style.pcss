.Wrapper {
  width: 100%;
  height: 100%;
  max-width: var(--main-max-width);
  float: left;
}
.selectedDate {
  font-family: var(--typography-fontFamily);
  font-style: normal;
  font-stretch: normal;
  font-size: 18px;
  text-align: left;
  margin-top: 5px;
}
